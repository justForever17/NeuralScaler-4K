[package]
name = "neuralscaler-core"
version = "2.2.1"
description = "NeuralScaler DLSS 5 Core Pipeline & IPC Daemon"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.35", features = ["full"] }
